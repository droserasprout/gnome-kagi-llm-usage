#!/usr/bin/env bash

cd ..

if [ -d kagi-llm-usage-extension ]; then
	rm -rf ~/.local/share/gnome-shell/extensions/kagi-llm-usage@drsr.io
	cp -r kagi-llm-usage-extension ~/.local/share/gnome-shell/extensions/kagi-llm-usage@drsr.io 
	cd ~/.local/share/gnome-shell/extensions/kagi-llm-usage@drsr.io/schemas
	glib-compile-schemas .
	gnome-session-quit --no-prompt
else
	echo "Extension folder not found"
fi
